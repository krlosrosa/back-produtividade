export interface {{pascalCase name}} {
  {{camelCase metodo}}: (params: {{pascalCase name}}.Params) => Promise<{{pascalCase name}}.Result>
}

export namespace {{pascalCase name}} {
  export type Params = {}
  export type Result = {}
}
